context.objects = [
  { factory = adapter
    args = {
      factory.name     = api.alsa.pcm.source
      node.name        = "aloop-capture"
      node.description = "YouTube dashboard capture (Loopback device 1)"
      media.class      = "Audio/Source"
      api.alsa.path    = "hw:Loopback,1"
      audio.channels   = 2
      audio.rate       = 44100
    }
  }
]
