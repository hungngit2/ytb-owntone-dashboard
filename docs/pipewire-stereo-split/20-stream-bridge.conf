context.objects = [
  { factory = adapter
    args = {
      factory.name        = support.null-audio-sink
      node.name           = "stream-bridge"
      node.description    = "YouTube dashboard stream bridge"
      media.class         = "Audio/Sink"
      audio.channels      = 2
      audio.position      = "FL,FR"
      monitor.passthrough = true
    }
  }
]
