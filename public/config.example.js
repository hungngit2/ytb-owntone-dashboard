// Copy this file to config.js (gitignored) and fill in your own key.
// Get one at https://console.cloud.google.com/apis/library/youtube.googleapis.com
// Restrict it (HTTP referrer or IP) in Google Cloud Console — this key is
// visible to anyone who views the page source.
const YOUTUBE_API_KEY = 'YOUR_API_KEY_HERE';

// Optional: number of upcoming tracks in queue to prefetch/resolve stream URLs for (default: 2)
// const STREAM_PREFETCH_QUEUE_COUNT = 2;
